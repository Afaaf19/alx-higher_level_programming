#!/usr/bin/node

// Script that prints 3 lines by using an array and a loop

const langs = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(langs[i]);
}
